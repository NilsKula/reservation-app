spring:
  datasource:
    username: nils
    password: nils
    url: jdbc:postgresql://localhost:5432/reservation_app
  jpa:
    hibernate:
      ddl-auto: validate
  liquibase:
    change-log: classpath:/liquibase/updates.xml
    enabled: true